[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    integration: hits live Azure (Search, Foundry, etc.); skipped unless AZURE_SEARCH_ENDPOINT (and friends) are set.
filterwarnings =
    ignore::DeprecationWarning:azure.*
    ignore::PendingDeprecationWarning
